#include <stdio.h>
/*
write a binary search function:
    binary search of array
    recieve integer array, starting index, ending index, and search key
    if search key is found, return array index
    if not found, return -1
*/
int main(void) {
    
}